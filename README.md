# biblioteca-sistema

Para iniciar el sistema es necesario tener xampp o wamp instalado en el computador
clonar el repositorio en la carpeta htdocs de xampp o en la carpeta www de wamp
ṕor ultimo, ejecutar el servidor apache y el phpmyadmin de xampp o wamp
